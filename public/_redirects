# Redirect default Netlify subdomain to primary domain
https://notifications.netlify.com/* https://octozero.app/:splat 301!

# Redirect all URLs to /index.html (Single Page App mode)
/*    /index.html   200